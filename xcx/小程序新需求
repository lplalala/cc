📄 第四期修复需求文档（直接给 Claude Code）
markdown
# 小程序最新问题修复与自检需求

## 一、退款失败：弹窗提示“sub_mch_id is empty.”

### 错误信息
调用退款接口时返回 `sub_mch_id is empty.`

### 原因分析
你正在使用**微信云开发**的 `cloud.cloudPay.refund` 进行退款，但该接口要求如果商户号是服务商模式下的子商户，则需要传入 `sub_mch_id`。  
由于你只有普通商户号（非服务商），不应当要求 `sub_mch_id`，出现此错误说明：
1. 云函数中调用 `cloud.cloudPay.refund` 时传入了空的 `sub_mch_id` 字段（或者不传但云开发环境默认要求）。
2. 或者云开发环境的支付配置中错误地设置了服务商参数。

### 修复方案
- **云函数退款代码修改**：
  - 检查调用 `cloud.cloudPay.refund` 的参数，确保**不要传入 `sub_mch_id`**（除非你真的是服务商子商户）。
  - 正确的最小参数集应为：
    ```js
    const result = await cloud.cloudPay.refund({
      out_trade_no: '订单号',
      total_fee: 原订单总金额（分）,
      refund_fee: 退款金额（分）
    })
注意：total_fee 和 refund_fee 单位都是分，且退款金额不能大于原订单金额。

环境配置：确认云开发环境 -> 设置 -> 微信支付配置中，商户号填写正确，且不勾选“服务商模式”。

测试验证：重新发起一笔1分钱真实支付订单，然后执行退款，应能成功原路退回，不再提示 sub_mch_id is empty.

二、注意事项页面优化：不展示图片，仅用放大标题
问题描述
在首页导航栏的注意事项页面中，目前显示了图片+标题，用户要求不展示图片，只用一个放大的标题来呈现注意事项。

修改位置
请找到以下可能包含注意事项的页面：

首页导航栏中的“注意事项”区域。


具体要求
移除所有 <image> 组件。

将注意事项的标题（如“注意事项”、“服务说明”）字体放大（例如 36rpx，粗体），颜色使用警示色（如橙色 #FF9800 或红色 #F44336）。

正文内容正常字号（28rpx），不再配图。

保持布局简洁、易读。

三、商城商品卡片右上角的“服务标识”样式调整
问题现象
目前商品列表每个商品卡片的右上角有一个“服务标识”，样式看起来很奇怪（字体和蓝色框不协调，蓝色框太大了）。

修复要求
重新设计标签样式：

调整蓝色框大小，字体大小，内边距，字体位置居中，不要超出图片边界，避免遮挡商品名称或价格。

四、点击“立即升级”显示“加载商品失败”
问题现象
在商品详情弹窗中，点击升级推荐的“立即升级”按钮，提示“加载商品失败”。

根本原因推测
后台商品数据结构中，upgrade_option 字段里存储的是升级版商品的 ID，但前端在点击时未能根据该 ID 正确查询到对应商品的信息。

你提到：“不清楚目标商品的 id 在哪里查看”。

修复方案
1. 数据关联完善
确保每个商品的 upgrade_option 中包含 target_id 字段，指向升级版商品的 _id。

后台管理（商品编辑页）中，添加一个选择器，从现有商品列表中选择哪个商品作为升级推荐，自动保存其 _id。

如果你没有后台管理页面，请 Claude 临时生成一个简单的商品编辑页（小程序内或云开发 web 端），用于绑定升级关联。

2. 前端逻辑修复
点击“立即升级”时，使用 upgrade_option.target_id 从云数据库的 products 集合中查询完整商品信息。

如果查询失败或 target_id 为空，应提示“暂无可升级的商品”而不是“加载商品失败”。

成功获取升级商品后，直接打开该商品的详情弹窗（或将升级商品加入下单流程）。

3. 如何查看目标商品的 ID
在云开发控制台 -> 数据库 -> products 集合中，每个商品都有一个 _id 字段，这就是商品 ID。

在后台管理商品时，可以直接复制这个 _id 填入升级推荐配置中。

也可以让 Claude 实现一个自动补全的下拉框，避免手动输入。

验收标准
后台可以配置商品 A 升级到商品 B（通过选择商品 B 的 ID）。

前端点击“立即升级”能正确显示商品 B 的详情，并能正常下单。

五、全面自检与 Bug 修复（Claude 自检）
在完成上述四项修复后，请 Claude 自动执行以下自检流程，并修复发现的问题：

自检清单
退款功能：

创建一笔真实支付订单（最低金额 0.01 元）。

调用退款，资金是否原路返回？订单状态是否更新为“已退款”？

不会再出现 sub_mch_id is empty.。

手机号显示（上一期问题）：

确认下单界面手机号是否完全显示，无遮挡。

升级图片上传（上一期）：

后台能否本地上传升级推荐商品的图片？前端是否正常显示？

退出登录（上一期）：

退出后重启小程序，是否仍处于未登录状态？能否正常切换账号？

本次修改点：

注意事项页面没有图片，只有放大标题。

商品卡片右上角标签样式美观、不怪异。

点击“立即升级”能正确加载商品，不会报错。

回归测试：

原有商城功能（列表浏览、搜索、下单、支付、订单管理、核销码）是否完好？

电子质保卡（种植终身质保逻辑）是否仍正确？

自检输出
Claude 需要输出一份自检报告，列出：

检查了哪些文件

发现并修复了哪些额外 Bug

每个修复点的验证方法

已知遗留问题（如有）

六、交付要求
修改后的完整代码（小程序端 + 云函数）。

如果有新增后台管理页面，提供访问路径或说明。

自检报告（以注释形式写在代码中或单独文档）。

重要提醒： 所有修改必须继续基于微信云开发，不得引入外部服务器。如果你不确定某个商品的 _id 在哪里查看，可以在代码中添加调试日志或控制台输出。